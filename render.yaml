services:
  - type: web
    name: tic-tac-toe-ai
    env: python3
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: PORT
        generateValue: true